---
name: Release

"on":
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.23'

      - name: Run tests
        run: go test ./... -race -count=1

      - name: Build binaries
        run: |
          GOOS=linux GOARCH=amd64 \
            go build -o build/regfilter-check-linux-amd64 ./cmd/regfilter-check
          GOOS=linux GOARCH=arm64 \
            go build -o build/regfilter-check-linux-arm64 ./cmd/regfilter-check
          GOOS=darwin GOARCH=amd64 \
            go build -o build/regfilter-check-darwin-amd64 ./cmd/regfilter-check
          GOOS=darwin GOARCH=arm64 \
            go build -o build/regfilter-check-darwin-arm64 ./cmd/regfilter-check

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: build/regfilter-check-*
          generate_release_notes: true
